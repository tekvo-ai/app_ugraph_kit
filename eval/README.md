# eval/

M0 stub. M1 will land:

- golden set construction (LLM candidates → hand-judged)
- judging rubric + disagreement log
- run-record schema (config hash, code SHA, metrics, cost/latency)
- results table with per-archetype breakdown
- golden-set biases doc

For now the M0 contract is that ingest exists and is testable; retrieval eval starts at M1.
